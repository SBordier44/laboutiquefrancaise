{% extends 'base.html.twig' %}

{% block body %}
    <h2>Confirmation de commande</h2>
    <hr>
    <p>
        Bonjour <strong>{{ order.user.fullName }}</strong>,<br><br>
        Nous vous remercions pour votre commande n°<strong>{{ order.reference }}</strong>.<br>
        Une confirmation de commande viens de vous être envoyée par email à l'adresse
        <strong>{{ order.user.email }}</strong>.
    </p>
    <hr>
    <p>
        Votre commande vous sera livrée par <strong>{{ order.carrierName }}</strong> à l'adresse suivante :
    </p>
    <div class="form-check">
        {{ order.delivery|raw }}
    </div>
    <hr>
    Pour suivre votre commande, rendez-vous dans votre <a href="{{ path('account_order') }}">compte</a>.
{% endblock %}

